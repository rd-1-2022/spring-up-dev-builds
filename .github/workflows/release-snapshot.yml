name: Release Snapshot

on:
  workflow_dispatch:

jobs:

  native:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - nickname: macos
            os: macos-latest
            graal: 22.0.0.2
          - nickname: linux
            os: ubuntu-latest
            graal: 22.0.0.2
    name: Build ${{ matrix.nickname }}
    steps:
      - uses: actions/checkout@v2
        with:
          repository: rd-1-2022/spring-up
      - uses: actions/setup-java@v2
        with:
          distribution: adopt
          java-version: 11
          cache: gradle
      - uses: graalvm/setup-graalvm@v1
        with:
          version: ${{ matrix.graal }}
          java-version: 11
          components: native-image
          set-java-home: false
          github-token: ${{ secrets.GITHUB_TOKEN }}
      - run: ./gradlew build nativeCompile --no-daemon
      - uses: actions/upload-artifact@v2
        with:
          name: spring-up-${{ matrix.nickname }}
          path: build/native/nativeCompile/
          retention-days: 1

  release:
    runs-on: ubuntu-latest
    needs: [native]
    name: Release
    steps:
      - uses: actions/download-artifact@v2
        with:
          name: spring-up-macos
          path: build/download/spring-up-macos
      - uses: actions/download-artifact@v2
        with:
          name: spring-up-linux
          path: build/download/spring-up-linux
      - name: Prepare Release
        run: |
          echo TAG=$(date +%Y%m%d-%H%m%S) >> $GITHUB_ENV
          mkdir build/upload
          mv build/download/spring-up-linux/spring-up build/upload/spring-up-linux-amd64
          mv build/download/spring-up-macos/spring-up build/upload/spring-up-darwin-amd64
      - uses: ncipollo/release-action@v1
        with:
          tag: ${{ env.TAG }}
          prerelease: true
          artifacts: "build/upload/*"
          token: ${{ secrets.GITHUB_TOKEN }}
